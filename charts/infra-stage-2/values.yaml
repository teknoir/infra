# Default values for infra-stage-2.
istioNamespace: istio-system

istiod:
  global:
    istioNamespace: istio-system

istio-ingressgateway:
  name: istio-ingressgateway
  labels:
    istio: ingressgateway
  service:
    type: LoadBalancer

istio-egressgateway:
  name: istio-egressgateway
  labels:
    istio: egressgateway
  service:
    type: ClusterIP
